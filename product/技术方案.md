
# 一、总体架构

```text
┌────────────────────┐
│   Mobile App       │  React Native (Expo)
└────────┬───────────┘~~~~
         │ HTTPS API
┌────────▼───────────┐
│   Backend API      │  Node.js (NestJS)
│   - 用户系统        │
│   - 记录系统        │
│   - AI调度层        │
└────────┬───────────┘
         │
┌────────▼───────────┐
│  Database          │  PostgreSQL
└────────┬───────────┘
         │
┌────────▼───────────┐
│   AI Service       │  LLM API + 决策逻辑
└────────────────────┘
```

原则：

* 前端只做 UI 与交互
* 后端掌控业务逻辑
* AI 服务独立，不写死在前端
* 数据结构稳定优先

---

# 二、前端架构（React Native + Expo）

## 技术栈

* React Native（Expo）
* TypeScript
* Zustand（状态管理）
* Axios（网络层）
* React Navigation
* Victory Native（图表）
* MMKV（本地缓存）

（饮食拍照识别相关建议）

* Expo Camera（拍照）
* Expo Image Picker（相册选择，可选）
* 直传对象存储（S3/R2/OSS 等）或后端中转上传（MVP 可先中转）

---

## 前端目录结构

```bash
src/
 ├── screens/
 │    ├── Home/
 │    ├── Record/
 │    ├── Trend/
 │    └── Profile/
 ├── components/
 ├── store/
 ├── services/
 │    └── api.ts
 ├── hooks/
 ├── utils/
 └── types/
```

---

## 核心页面结构

### 1️⃣ 首页（核心心智入口）

模块：

* 今日健康状态卡
* AI 今日建议
* 快速记录按钮
* 本周趋势小图

首页必须是“状态 + 建议”，而不是“历史记录列表”。

---

### 2️⃣ 记录页

* 饮食记录
* 运动记录
* 快速输入
* 模板化录入

#### 饮食记录（新增：拍照识别热量）

前端需要支持三条链路：

* **营养价值表识别（优先）**：拍照营养成分表 → OCR 识别 → 输入食用克数 → 自动计算 → 保存
* **参考物拍照识别**：拍照（食物+拳头） → 上传 → 返回候选食物/估算热量 → 用户确认/修改 → 保存
* **快捷模式（pending）**：拍照 → 仅存档 → 稍后补录克数/选择食物 → 重算 → 保存确认

关键点：

* **营养价值表优先**：包装食品优先引导用户拍摄营养成分表，数据更准确
* **参考物/对照物**：首次使用时引导录入（拳头 + 硬币，仅需一次），拍照估算时必须同框拍到食物和拳头，用于份量估算稳定性
* **失败兜底**：识别失败或置信度低时，允许一键转快捷模式，避免阻塞用户记录

##### 前端模块拆分（建议）

* `screens/Record/DietModeSelectScreen`：选择识别模式（营养价值表/参考物拍照/快捷模式）
* `screens/Record/DietCameraScreen`：相机拍照/相册选取、权限请求
* `screens/Record/NutritionLabelScreen`：营养价值表 OCR 识别结果、输入克数、计算热量
* `screens/Record/DietAnalyzeScreen`：上传进度、识别请求、loading/重试
* `screens/Record/DietResultScreen`：候选列表、增删食物、编辑克数/份数、保存
* `screens/Record/DietRecordDetailScreen`：pending → confirmed 补录入口
* `screens/Record/ReferenceSetupScreen`：首次参考物录入（拳头 + 硬币）
* `services/diet.ts`：上传/识别/保存相关 API 封装
* `types/diet.ts`：DietRecord/DietItem/Reference/NutritionLabel 等类型定义

##### 状态机（避免边缘状态混乱）

建议把 diet 记录的关键状态明确为：

* `draft_local`：本地草稿（拍完照但还没拿到上传 URL / 网络中断）
* `pending`：已存档（有图片 URL），未确认 items/total
* `confirmed`：已确认（items/total 完整）

本地草稿可用 MMKV 暂存（最多保留 N 条），避免用户在上传/识别过程中切后台导致丢失。

---

### 3️⃣ 趋势页

* 周/月趋势
* 简单折线图
* 不做复杂数据分析

---

### 4️⃣ 个人页

* 基础信息
* 目标设置
* 提醒开关

---

# 三、后端架构（NestJS）

推荐使用：

> NestJS + PostgreSQL

理由：

* 结构清晰
* 模块化
* 可扩展
* 类型安全

---

## 后端目录结构

```bash
src/
 ├── modules/
 │    ├── user/
 │    ├── record/
 │    ├── ai/
 │    ├── trend/
 ├── common/
 ├── config/
 └── main.ts
```

---

# 四、数据库设计（核心表结构）

---

## 1️⃣ users

```sql
id (uuid)
email
password_hash
nickname
age
gender
height
weight
goal_type
created_at
```

---

## 2️⃣ health_records

```sql
id (uuid)
user_id
type (diet / workout)
value_json (JSONB)
created_at
```

value_json 示例：

```json
{
  "calories": 500,
  "protein": 30,
  "duration": 40,
  "intensity": "medium"
}
```

灵活扩展，不锁死字段。

---

## 3️⃣ daily_state

用于首页快速计算。

```sql
id
user_id
date
calorie_total
workout_minutes
score
```

---

## 4️⃣ ai_recommendations

```sql
id
user_id
date
recommendation_text
model_version
```

---

# 五、AI 架构设计（核心差异点）

AI 不直接生成最终答案。

设计三层：

---

## 1️⃣ 数据聚合层

后端整理用户：

* 过去7天记录
* 目标
* 体重趋势
* 今日完成情况

生成结构化 Prompt。

---

## 2️⃣ LLM 调用层

调用模型生成：

* 今日优先行动
* 轻度反馈
* 风险提醒

---

## 3️⃣ 决策约束层（重要）

对 AI 输出进行规则校验：

* 不允许过激建议
* 不允许医疗建议
* 不允许极端饮食

AI 是建议器，不是医生。

---

# 六、API 设计

---

## 用户

```http
POST   /auth/register
POST   /auth/login
GET    /user/profile
PUT    /user/profile
```

---

## 记录

```http
POST   /record
GET    /record/list
```

### 饮食拍照识别（新增）

说明：前端是 Expo/RN，建议采用「后端签名直传对象存储」或「后端中转上传」两种模式之一；MVP 可先后端中转，后续切签名直传降低带宽成本。

#### 1) 营养价值表 OCR 识别（优先模式）

```http
POST /diet/nutrition-label/analyze
```

请求：

```json
{
  "image_url": "https://cdn.../xxx.jpg",
  "meal_type": "breakfast"
}
```

返回（示例）：

```json
{
  "status": "ok",
  "confidence": "high",
  "nutrition_per_100g": {
    "calories": 450,
    "protein": 12,
    "fat": 20,
    "carbohydrate": 55,
    "sodium": 800
  },
  "detected_fields": ["calories", "protein", "fat", "carbohydrate"],
  "warnings": []
}
```

用户输入实际食用克数后，前端计算实际摄入：

```javascript
actual_calories = (nutrition_per_100g.calories * grams) / 100
```

#### 2) 获取上传凭证（推荐：直传对象存储）

```http
POST /diet/photo/upload-url
```

返回：

```json
{
  "upload_url": "https://...",
  "file_url": "https://cdn.../xxx.jpg",
  "expires_in": 600
}
```

#### 2) 发起识别（标准模式）

```http
POST /diet/photo/analyze
```

请求：

```json
{
  "image_url": "https://cdn.../xxx.jpg",
  "meal_type": "breakfast",
  "reference": {
    "type": "fist",
    "present_in_image": true
  }
}
```

返回（示例）：

```json
{
  "status": "ok",
  "confidence": "medium",
  "total": { "calories": 520 },
  "items": [
    {
      "name": "炒鸡蛋",
      "candidates": ["炒鸡蛋", "煎蛋"],
      "estimated_grams": 120,
      "calories": 210
    }
  ],
  "warnings": ["no_reference_detected"]
}
```

（约束建议）

* `warnings` 用于前端 UI 提示（例如「未检测到参考物」「菜品重叠」），不影响保存，但应引导用户补录克数校准。
* `confidence` 只做解释与引导，不做强阻断（避免高弃用率）。

#### 4) 创建/更新饮食记录（复用 /record，但建议明确结构）

`/record` 的 `value_json`（diet）建议扩展为：

**营养价值表模式**：

```json
{
  "mode": "nutrition_label",
  "status": "confirmed",
  "meal_type": "breakfast",
  "images": [{ "url": "https://cdn.../nutrition_label.jpg" }],
  "nutrition_per_100g": {
    "calories": 450,
    "protein": 12,
    "fat": 20,
    "carbohydrate": 55
  },
  "consumed_grams": 80,
  "total": {
    "calories": 360,
    "protein": 9.6,
    "fat": 16,
    "carbohydrate": 44
  },
  "source": "nutrition_label_ocr"
}
```

**参考物拍照模式**：

```json
{
  "mode": "reference_photo",
  "status": "confirmed",
  "meal_type": "breakfast",
  "images": [{ "url": "https://cdn.../xxx.jpg" }],
  "confidence": "medium",
  "items": [
    {
      "name": "炒鸡蛋",
      "grams": 120,
      "calories": 210,
      "source": "ai"
    }
  ],
  "total": { "calories": 520 },
  "reference": { "type": "fist", "present_in_image": true }
}
```

**快捷模式（pending）**：

```json
{
  "mode": "quick",
  "status": "pending",
  "meal_type": "breakfast",
  "images": [{ "url": "https://cdn.../xxx.jpg" }],
  "note": ""
}
```

（安全与合规建议）

* 图片 URL 建议为短期可访问（签名 URL）或走 CDN + 鉴权；不要直接暴露永久公网可读原图。
* 记录删除时，若需要删除图片对象，应在后端做异步清理（并对失败重试）。

#### 5) 参考物配置（仅需一次）

```http
PUT /user/profile
```

在用户资料里保存参考物（示例）：

```json
{
  "diet_reference": {
    "fist": {
      "image_url": "https://cdn.../fist.jpg",
      "calibrated": true
    },
    "coin": {
      "image_url": "https://cdn.../coin.jpg",
      "diameter_mm": 25,
      "calibrated": true
    }
  }
}
```

说明：拳头和硬币都需要录入，拳头用于大份量食物对比，硬币用于小份量食物对比。

---

## 首页

```http
GET    /dashboard
```

返回：

```json
{
  "today_state": {...},
  "ai_recommendation": "...",
  "weekly_summary": {...}
}
```

---

## 趋势

```http
GET /trend/weekly
GET /trend/monthly
```

---

# 七、开发阶段规划（现实版本）

---

## 阶段 1（2周）

* 数据库搭建
* 用户系统
* 记录接口
* 基础 dashboard API

---

## 阶段 2（2周）

* AI 服务接入
* 首页完成
* 记录流程完成

（补充）在阶段 2 的「记录流程完成」里包含：

* 饮食拍照：相机/相册选取、上传、识别结果页、快捷模式
* 参考物录入（拳头默认 + 自定义对照物）

（补充）联调与压测点：

* 弱网/超时：上传重试、识别重试、草稿兜底
* 大图：上传前压缩/限制分辨率（后端也要限制 size）
* 成本：优先走直传，后端只接收 image_url + 元信息

---

## 阶段 3（2周）

* 趋势页
* 优化体验
* 简单动画

---

## 阶段 4（2周）

* 上架准备
* Bug 修复
* 性能优化

---

# 八、为什么这样设计？

因为你一个人。

这个架构满足：

* 不复杂
* 不依赖原生模块
* 不做过度微服务
* 可扩展

---

# 九、未来扩展方向

当用户增长后：

* Redis 做缓存
* 队列处理 AI 请求
* 事件驱动架构
* AI 模型版本管理
* 多策略推荐系统

但现在不要做。

---

# 十、最终建议（非常关键）

你要记住：

这个产品的本质不是“记录”。

而是：

> 用 AI 帮用户形成稳定健康节律。

技术架构必须服务这个目标。

---

如果你愿意，下一步我可以：

* 给你数据库完整 SQL
* 帮你设计 AI Prompt 结构
* 或直接给你一个 NestJS 项目初始化结构模板
